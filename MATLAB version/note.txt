"footprint.m" is the main command file, while foot4,5,6,7.m are the belonged files;

Just input "footprint" in MATLAB command window to run the analysis;

This program is corresponding to the Visual C++ program version 2.0, however, seems with different algorithms;

User can change the initial values in line 32, 41, 53 and 66 by editing the main file;



"footprint3.m" and its belonged files are corresponding to the Visual C++ program version 3.0. The usage is the same.


